package com.fb.common.enums;

/**
 * Các loại phản ứng emoticon trên bài viết và bình luận.
 *
 * Cho phép người dùng bày tỏ cảm xúc đa dạng
 * đối với nội dung, tương tự hệ thống reactions trên Facebook.
 */
public enum ReactionType {
    /** Thích (👍) */
    LIKE,
    /** Yêu thích (❤️) */
    LOVE,
    /** Cười (😂) */
    HAHA,
    /** Ngạc nhiên (😮) */
    WOW,
    /** Buồn (😢) */
    SAD,
    /** Giận dữ (😡) */
    ANGRY
}
